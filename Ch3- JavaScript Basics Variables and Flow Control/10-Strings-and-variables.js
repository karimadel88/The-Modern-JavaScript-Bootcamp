/* we used let as reversed keyword to store data */
/* Syntax : [ let nameVar (=) value ]  */

/**
 * variable to store a name
 */
let myName = 'karim'

/**
 * variable to store a Sentence
 */
let sen = 'student at auc'

/**
 * console.log() => is used to print
 */
console.log(myName)

let firstName = 'Karim'
let lastName ='Adel'
/* We can use plus operator to combine between two words or more */
let fullName = firstName + " " + lastName

console.log(fullName)
/* Semicolons is optional in js and prfer use it in the end of file */