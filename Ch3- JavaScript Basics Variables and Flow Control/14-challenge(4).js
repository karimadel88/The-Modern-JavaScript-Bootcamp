/**
 * Creat age variable
 * Calculate is child - if they are 7 or under
 * Calculate is senior - if they are 65 or older
 * print is child value
 * print is senior value 
 */
let age = 22
let isChild = age <= 7
let isSenior = age>=65

console.log(isChild)    // will print false
console.log(isSenior)   // will print false