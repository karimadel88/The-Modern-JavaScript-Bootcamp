// Boolean Data Type

/* (===) => is equality operator
   (!==) => no equal operator
*/
let temp = 31
// let isFreezing = true // or false
let isFreezing = temp === 31
console.log(isFreezing) // will print true

isFreezing = temp == 32 
console.log(isFreezing) // will print false 

isFreezing = temp!==32 // will prinr true


let name = "karim"
let isAhmed = name === 'ahmed'
console.log(isAhmed) //will print false

/**
 * greater than >
 * less than <
 * greater than or equal to >=
 * less than or equal to <=
 */
let isFreezing = temp > 32
console.log(isFreezing)     // will print false
let isFreezing = temp < 32  // will print true
