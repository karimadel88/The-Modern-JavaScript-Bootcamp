// const notes = [data type ==> string, numbers, Boolean, Object, Array and else ..]

// Sting array
const notes = ['Note 1', 'Note 2', 'Note 3']
console.log(notes)

// size of array by length property
let size = notes.length
console.log(size)

// print by 0-index
let firstItem = notes[0]
console.log(firstItem)

// edit by index of array
notes[3] = 'Note 4'
console.log(notes[3])

// get last item by length
console.log(notes[notes.length-1])